#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * **strtow-this is the main function
 *
 * It splits a string into words
 * @str:the string to be splitted
 * Return: returns a pointer to an array of strings,
 * NULL if str == NULL or str == "", or if the function fails
 */
char **strtow(char *str)
{

