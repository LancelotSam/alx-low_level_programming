#include <stdio.h>
#include "main.h"
/**
 * main-print alphabet in lowercase
 * Return: 0(success)
 */
void print_alphabet(void);
int main(void)
{
int alp;
for (alp = a; alp <= z; alp++)
{
putchar(alp);
}
putchar('\n');
return (0);
}
